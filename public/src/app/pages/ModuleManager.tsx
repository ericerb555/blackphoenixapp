// ModuleManager.tsx - Stub page for Module Manager
// This route exists but the page needs to be created
import { StubPage } from '../components/StubPage';

export default function ModuleManager() {
  return (
    <StubPage 
      title="Module Manager"
      description="Manage application modules, permissions, and feature access"
    />
  );
}
